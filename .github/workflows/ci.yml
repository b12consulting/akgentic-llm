name: CI

on:
  push:
  pull_request:
    branches: [master]
  workflow_dispatch:

env:
  NO_COLOR: "1"

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout workspace
        uses: actions/checkout@v6
        with:
          repository: b12consulting/akgentic-quick-start
          submodules: recursive
          token: ${{ secrets.GH_PAT }}

      - name: Override akgentic-llm submodule with current branch
        working-directory: packages/akgentic-llm
        run: |
          git fetch origin ${{ github.head_ref || github.ref_name }}
          git checkout FETCH_HEAD

      - name: Install uv
        uses: astral-sh/setup-uv@v8.1.0
        with:
          version: "latest"

      - name: Set up Python 3.12
        run: uv python install 3.12

      - name: Install dependencies
        run: uv sync --all-packages --all-extras

      - name: Type checking
        run: uv run mypy packages/akgentic-llm/src/

      - name: Linting
        run: uv run ruff check packages/akgentic-llm/src/

      - name: Run tests with coverage
        run: >
          uv run pytest packages/akgentic-llm/tests/
          --cov=akgentic.llm
          --cov-report=term-missing
          --cov-report=json:coverage.json
          --cov-fail-under=80
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}

      - name: Update coverage badge
        if: github.ref_name == 'master' && github.event_name == 'push'
        run: |
          COVERAGE=$(python -c "import json; print(int(json.load(open('coverage.json'))['totals']['percent_covered_display']))")
          if [ "$COVERAGE" -ge 90 ]; then COLOR="brightgreen"
          elif [ "$COVERAGE" -ge 80 ]; then COLOR="green"
          elif [ "$COVERAGE" -ge 70 ]; then COLOR="yellow"
          else COLOR="red"; fi
          echo "{\"schemaVersion\":1,\"label\":\"coverage\",\"message\":\"${COVERAGE}%\",\"color\":\"${COLOR}\"}" > badge.json
          gh gist edit dd80a44fe9e2e27b46f7f3431e19202f -f coverage.json badge.json
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}
